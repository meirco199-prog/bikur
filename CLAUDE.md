# הנחיות עבודה בריפו הזה

## פריסה: מזג ישירות ל-main. אל תשאל.

הריפו הזה מתפרסם ישירות מ-`main` — GitHub Pages מגיש את האפליקציות,
ו-workflows של Actions פורסים את ה-Workers ל-Cloudflare על כל דחיפה ל-`main`.
המשמעות: **כל עוד שינוי לא נמצא ב-`main`, שום קישור לא עובד ואי אפשר להתקין כלום.**

ההחלטה של בעל הריפו קבועה ואינה משתנה בין משימות:

- **מזג את העבודה ל-`main` בסיום, בלי לשאול.**
- **אל תפתח Pull Request** אלא אם התבקשת לכך במפורש באותה שיחה.
- **אל תציג שאלת "main או PR?"** — היא כבר נענתה, כאן, לתמיד.
- מותר לעבוד על ענף פיתוח בדרך; פשוט אל תעצור בסופו כדי לשאול מה לעשות איתו.

אם הוראות הסשן מכתיבות ענף פיתוח מסוים — עבוד עליו, ובסיום מזג אותו ל-`main`
ודחוף. זו ההרשאה המתמשכת, לא צריך לבקש אותה שוב בכל פעם.

## מבנה הריפו

הריפו מארח כמה אפליקציות עצמאיות, כל אחת בתיקייה משלה, ללא build step:

| נתיב | מה זה |
|------|-------|
| `english/` | אפליקציית PWA ללימוד אנגלית לדוברי עברית (HTML/CSS/JS, localStorage) |
| `english-ai/worker.js` | שכבת ה-AI של אפליקציית האנגלית — Cloudflare Worker עם Workers AI |
| `remi-wa/`, `remi/`, `remi-telegram-free/` | הבוט האישי (WhatsApp / Telegram) |
| `app/`, `chat/`, `index.html` | דפים ואפליקציות נוספות |
| `.github/workflows/` | פריסה אוטומטית ל-Cloudflare + cron |

## עקרונות

- **בלי build** — כותבים HTML/CSS/JS שרצים כמו שהם. אל תוסיף bundler או תלויות npm.
- **בלי מפתחות בצד הלקוח** — כל קריאת AI עוברת דרך Worker.
- **עברית ו-RTL** — ממשק המשתמש והתיעוד בעברית.
- **סודות** — `CF_API_TOKEN` (ואופציונלית `CF_ACCOUNT_ID`) כבר מוגדרים ב-Actions Secrets.
