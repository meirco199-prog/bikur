# AI Council — דיון טכני בין Claude ל-ChatGPT

המסמך הזה הוא פרוטוקול העבודה בין שני מודלים על פלטפורמת ההשקעות, ותיעוד הדיונים עצמם. **לא מודל של
"ChatGPT מורה, Claude מבצע"** — שני "מהנדסים" מציגים עמדה מגובה בקוד/נתונים, בעל הריפו מכריע, ובמחלוקות
אמיתיות בונים ניסוי (Shadow/A-B) שמכריע עם תוצאות, לא עם ויכוח תיאורטי.

## כללי העבודה (קבועים, לא לשנות בלי אישור בעל הריפו)

1. **אל תניח שביקורת חיצונית (ChatGPT או אחר) נכונה אוטומטית.** תמיד לבדוק מול הקוד, הבדיקות, והארכיטקטורה בפועל.
2. אם מסכימים — להסביר למה, ולהביא ראיה קונקרטית (קובץ+שורה, תוצאת בדיקה, נתון).
3. אם לא מסכימים — לומר זאת **במפורש**, להסביר למה, ולהציג חלופה. אי-הסכמה מנומקת עדיפה על הסכמה מנומסת.
4. **שתי גישות סבירות ≠ בוחרים אחת ומוחקים את השנייה.** מציעים A/B test, מסלול Shadow נוסף, או בדיקה שתאפשר להשוות.
5. להפריד תמיד בין חמש קטגוריות שונות של ממצא:
   - **באג מוכח** — יש הדגמה (בדיקה שנכשלת בלי התיקון, או ריצה שמראה תוצאה שגויה).
   - **סיכון אפשרי** — לא הודגם, אבל תרחיש סביר שצריך הגנה/בדיקה.
   - **החלטת ארכיטקטורה** — שתי דרכים תקינות, שונה רק בטרייד-אוף (למשל: לעצור קניית ליבה בשוק דובי או לא).
   - **השערה על אסטרטגיית השקעה** — טענה שרק מדידה תוכיח ("regC יכה את SPY"), לא נבדקת בקוד.
   - **שינוי שמוכח בנתונים** — תוצאה ממדידה בפועל (Shadow/A-B) שמצדיקה שינוי.
6. **אין לשנות משקלי השקעה, מגבלות סיכון, או מודל בחירה רק כדי "לרצות" ביקורת.** שינוי כזה דורש נימוק ודיון
   מפורש בקובץ הזה, לא רק commit שקט.
7. בכל מחלוקת אמיתית: לכתוב איפה ChatGPT צודק, איפה הוא טועה (מנקודת המבט של Claude), ומה Claude היה עושה
   במקום — ולהפך, בעל הריפו יכול לכתוב איפה Claude צודק.
8. **בלי סודות בקובץ הזה.** לא tokens, לא credentials, לא ערכי CRON_SECRET/APP_TOKEN — אפילו לא "לדוגמה".
   כשמדברים על סוד שנחשף (עבר/הווה), מנסחים במדויק: "אין ערך סוד פעיל בקבצים הנוכחיים" ≠ "הערך לא קיים
   בשום מקום" — הוא נשאר לצמיתות בהיסטוריית git ובשיחות עבר, ופשוט נחשב compromised ופסול לשימוש.
9. כשעולה שינוי קוד מתוך דיון כאן, ה-commit מציין איזה Issue הוביל אליו (למשל "AI_COUNCIL#3").
10. **בהחלטת ארכיטקטורה או השערת אסטרטגיה** — לפני שרואים תוצאות, **Claude ו-ChatGPT** (שני הצדדים בדיון)
    כותבים כל אחד "What would change my mind" משלו (שדות נפרדים בתבנית למטה). בעל הריפו **אינו** "מהנדס
    שני" בדיון עצמו — הוא זה שמכריע, וממלא את שדה `Owner decision`. המטרה: למנוע מצב ששני הצדדים בדיון
    מגנים על העמדה ההתחלתית שלהם גם כשהנתונים כבר מראים אחרת.

## תבנית לנושא חדש

**מבנה סימטרי (מ-AI_COUNCIL#11 והלאה):** Claude ו-ChatGPT הם שני מהנדסים שווי-מעמד, לא "Claude עם עמדה
ראשית + ChatGPT כ-Counterargument". בעל הריפו הוא זה שמכריע — לא Claude. שדה `ChatGPT position` הוא
כפי שבעל הריפו מעביר אותו (ChatGPT לא כותב ישירות לקובץ הזה); אם הניסוח לוקה בתמצות/אובדן ניואנס
בהעברה, זה נרשם ב-`Where we disagree` ולא מוצג כעמדה סופית של ChatGPT.

```
### AI_COUNCIL#<n>: <כותרת קצרה>
**קטגוריה:** באג מוכח / סיכון אפשרי / החלטת ארכיטקטורה / השערת אסטרטגיה / שינוי מבוסס-נתונים
**Issue:** <מה הבעיה/השאלה, בקצרה>
**Claude position:** <העמדה של Claude ולמה>
**ChatGPT position:** <העמדה של ChatGPT כפי שהועברה על ידי בעל הריפו>
**Evidence from code/data:** <קובץ+שורה, תוצאת בדיקה, נתון מדוד>
**Where we agree:** <נקודות שבהן שתי העמדות מתלכדות>
**Where we disagree:** <נקודות מחלוקת אמיתיות, בלי לטשטש>
**Proposed experiment/fix:** <תיקון ישיר / A-B / מסלול Shadow נוסף / בדיקה שתכריע>
**Risks:** <מה עלול להשתבש מהתיקון/מהניסוי עצמו>
**What would change Claude's mind:** <חובה בהחלטת ארכיטקטורה/השערת אסטרטגיה — איזה נתון/תוצאה ישכנע
את Claude לשנות עמדה, לפני שרואים תוצאות>
**What would change ChatGPT's mind:** <אותו דבר, מנקודת המבט של ChatGPT (כפי שהועבר)>
**Owner decision:** <ההכרעה של בעל הריפו — או "טרם הוכרע" אם עוד לא הגיע הזמן>
**Status:** OPEN / AGREED / TESTING / RESOLVED
```

נושאים #1–#10 נכתבו לפי התבנית הקודמת (עמדת Claude + Counterarguments, "What would change my mind"
זוגי ל-Claude/בעל-הריפו) ונשארים כפי שהם כתיעוד היסטורי — לא שוכתבו רטרואקטיבית, חוץ מ-#5 (ראה #11).

---

## יומן הנושאים

### AI_COUNCIL#1: מכירת SPY כפולה ב-decideAggressiveB
**קטגוריה:** באג מוכח
**Issue:** מכירת ה-SPY המקדימה (למימון קניות DF כשאין מזומן פנוי) הוחלה ישירות על ה-state שגם הוחזר/נשמר
ל-KV לפני המילוי בפועל, ואז `applyFills` במילוי האמיתי למחרת ביצע את אותה מכירה שוב.
**Claude position:** באג אמיתי, לא תיאורטי — נבדק ואומת.
**Evidence from code/data:** `engine/tracks.js` (לפני התיקון) — `st0.cashIls +=`/`p.qty -=` הוחל ישירות על
העותק שהוזרק ל-`decideAggressive` וחזר כ-`d.state`. בדיקת רגרסיה חדשה ב-`tests/tracks-lib.test.mjs`
("מכירת SPY מקדימה למימון קניות מבוצעת פעם אחת בדיוק") מאמתת: בלי התיקון, 240 יחידות SPY יורדות ל-140
(במקום ל-215) אחרי decide+fill יחיד. עם התיקון: 240→215, פעם אחת.
**Counterarguments:** אין — זוהה ואומת עצמאית בביקורת קוד חיצונית (ChatGPT) ואושש בבדיקה.
**Proposed experiment/fix:** תוקן ישירות (לא דורש A/B — זה תיקון תקינות, לא החלטת אסטרטגיה). המכירה המקדימה
מוחלת רק על עותק פנימי שמוזן להחלטה, ומבוטלת במדויק מה-state המוחזר לפני השמירה.
**Risks:** נמוך — הבדיקה מכסה את התרחיש המדויק; שאר בדיקות ה-tracks (156) לא נשברו.
**Status:** RESOLVED (commit `7953bc7`)

### AI_COUNCIL#2: DF coverage fail-open
**קטגוריה:** באג מוכח
**Issue:** `eligibleDF` קיבל מניה ככשירה כש-`coverage` לא היה מספר בכלל (undefined/null), במקום לדרוש כיסוי
ידוע ומספיק — כלומר "אין מידע" עבר כאילו "יש מידע טוב".
**Claude position:** באג אמיתי — fail-open בדיוק במקום שאמור להיות fail-closed (סף איכות).
**Evidence from code/data:** `engine/shadow.js`, התנאי היה `(!isNum(stocks[i].coverage) || stocks[i].coverage >= DF_RULES.minCoverage)`.
בדיקה חדשה ב-`tests/shadow.test.mjs` מאמתת ש-`coverage: undefined` נכשל בסינון אחרי התיקון.
**Counterarguments:** אין.
**Proposed experiment/fix:** תוקן ישירות ל-`isNum(...) && ... >= minCoverage`.
**Risks:** נמוך — משנה רק כשירות DF, לא נוגע ב-D המקורי (D-preRevision/D נשארים ללא שינוי).
**Status:** RESOLVED (commit `7953bc7`)

### AI_COUNCIL#3: יום מילוי — סוף שבוע/חג
**קטגוריה:** באג מוכח, בשני סבבים
**Issue:** הסריקה הלילית מתייגת יום לפי `today()` גולמי (UTC) — ביום חול זה תואם בדיוק ליום המילוי, אבל
אחרי סגירת שישי היא רצה מוקדם שבת ומתויגת "שבת", בזמן שמחיר 09:40 האמיתי (entry940) קיים רק בשני.
**Claude position (סבב 1):** תיקון ראשון (`isExpectedFillDay` עם טווח gap≤4) פתר את חסימת שבת→שני, אבל
היה **רופף מדי**: התיר גם שבת→שלישי, גם כששני היה יום מסחר תקין.
**ChatGPT position (סבב 2):** `isExpectedFillDay` חייב לאכוף את יום המסחר הבא **בדיוק**, לא כל יום בטווח.
**Claude position (סבב 2):** מסכים לגמרי — הביקורת נכונה וזיהתה חולשה אמיתית בתיקון הראשון של Claude עצמו.
**Evidence from code/data:** `engine/session.js` — הוחלף ב-`nextTradingDay()` (הליכה יום-יום, מדלג סופ"ש/חג)
ו-`isExpectedFillDay` דורש התאמה מדויקת. בדיקה ב-`tests/tracks.test.mjs` מאמתת ששבת→שלישי נדחה כשני תקין.
**Counterarguments:** אין נותר — הפתרון הנוכחי סוגר את שני הכיוונים (לא חוסם לגיטימי, לא מאשר מילוי מאוחר מדי).
**Proposed experiment/fix:** תוקן ישירות (תיקון תקינות, לא אסטרטגיה).
**Risks:** לוח `NYSE_HOLIDAYS` הוא רשימה קבועה שדורשת עדכון שנתי (כמו `SPY_SECTOR_WEIGHTS`) — חג שלא יעודכן
פשוט לא ייהנה מהחריגה (fail-closed, לא fail-open — סיכון נמוך).
**Status:** RESOLVED (commit `e86723a`)

### AI_COUNCIL#4: מילוי בדיעבד במחיר שכבר היה ידוע (decided-after-fill-window)
**קטגוריה:** סיכון אפשרי → תוקן מונע
**Issue:** אין הגנה שמוודאת שההחלטה עצמה (decidedAt) נוצרה לפני 09:40 ניו יורק של יום המילוי — הרצה ידנית
מאוחרת הייתה יכולה "לבצע" במחיר שכבר היה ציבורי כשההחלטה התקבלה (look-ahead).
**Claude position:** סיכון אמיתי לאמינות המדידה, גם אם לא הודגם כתקלה שקרתה בפועל בפרודקשן.
**Evidence from code/data:** `engine/session.js` — `decidedBeforeFillWindow()` חדש; `engine/tracks.js`
`preflight()` קוד חדש `decided-after-fill-window`. בדיקות יחידה+אינטגרציה מכסות זמן תקין/מאוחר מדי.
**Counterarguments:** אין.
**Proposed experiment/fix:** תוקן מונע (guard), לא דורש A/B.
**Risks:** בדיקות שמדמות ימים בדויים (לא הזמן האמיתי) חייבות "לתארך אחורה" את `decidedAt` בעצמן, אחרת
ההגנה תחסום גם ריצות בדיקה לגיטימיות — תוקן ב-`tracks-lib.test.mjs`/`tracks-worker.test.mjs`/`mock-server.mjs`.
**Status:** RESOLVED (commit `e86723a`)

### AI_COUNCIL#5: קניית ליבת SPY בשוק דובי — להמשיך או לעצור?
**קטגוריה:** החלטת ארכיטקטורה (לא באג)
**Issue:** ב-`decideAggressive`, קניית מניות בודדות נעצרת בשוק דובי, אבל השלמת ליבת SPY ל-20% ממשיכה בכל
משטר. תיעוד ישן ("שוק דובי: אין קניות") לא הבחין בין השניים ונקרא כסותר את הקוד.
**Claude position:** להשאיר את הקוד כמו שהוא (ממשיך להשלים ליבה גם בשוק דובי) ולתעד במדויק — לא לשנות
התנהגות רק בגלל שהניסוח הישן היה מטעה. המערכת נבנתה כדי **להשוות** אסטרטגיות, ושינוי התנהגות עכשיו, בלי
מדידה, הורס בדיוק את מה שרוצים למדוד.
**ChatGPT position:** בעד לא לשנות את המסלול הקיים ולהמשיך למדוד אותו כפי שהוא. ישנה עמדה לטובת עצירת
השלמת SPY בשוק דובי אם מסלול Shadow זהה לחלוטין, שההבדל היחיד בו הוא עצירת קניית הליבה בזמן Bear Trend,
יראה לאורך כמה משטרי שוק שיפור עקבי ב-max drawdown וב-Sortino/return-to-risk, בלי לפגר בצורה מהותית
בתשואה המצטברת או בהתאוששות לאחר המעבר חזרה ל-Bull. 60 יום בשוק אחד לבדו אינם הוכחה מספקת.
**Evidence from code/data:** `engine/aggressive.js` שלב 4 ("ליבה: SPY עד 20%") לא בתוך ה-`if(bear)` שמכבה
מניות בודדות בשלב 5.
**Where we agree:** לא לשנות את המסלול הקיים כרגע; שינוי בפועל (אם בכלל) דרך מסלול Shadow נפרד, לא שינוי
בקיים; מדגם קצר/משטר שוק אחד לא מספיק כדי להכריע; המדד הרלוונטי הוא יחס תשואה-לסיכון (drawdown/Sortino),
לא "פחות תנודתי" בלבד.
**Where we disagree:** אין מחלוקת מהותית כרגע — שתי העמדות מסכימות על אי-שינוי והמתנה לניסוי. הניסוח של
ChatGPT מחמיר יותר על מספר משטרי השוק הנדרשים ("כמה משטרים", לא רק 60 יום אחד) מהניסוח המקורי של Claude.
**Proposed experiment/fix:** תועד ב-`SHADOW_MODEL.md` שההגבלה חלה על מניות בודדות בלבד. מסלול Shadow עתידי
(אם ייפתח): זהה לחלוטין ל-`aggrB`/`aggr` חוץ מסעיף אחד — משלים/לא-משלים ליבה בשוק דובי — כדי שההשוואה
תבודד את המשתנה הזה בלבד ולא תערבב אותו עם שינויים אחרים.
**Risks:** אין נכון לעכשיו — תיעוד בלבד, קוד לא שונה. מסלול Shadow נוסף עתידי מוסיף עוד רקורד להשוואה
ועוד עומס תחזוקה קטן (עוד track ב-`TRACKS`), לא סיכון כספי (Shadow בלבד).
**What would change Claude's mind:** אשנה עמדה לטובת "שווה לפתוח מסלול Shadow שעוצר גם רכישת ליבה בשוק
דובי" אם, לאורך תקופת מדידה שכוללת משטר דובי ממשי אחד לפחות (לא רק תנודתיות רגילה, מינימום 60 יום),
מסלול כזה יראה `maxDrawdown` נמוך משמעותית מהקיים בלי לפגר מהותית בתשואה המצטברת/`excess.own`/
`excess.common` מול SPY.
**What would change ChatGPT's mind:** שיפור עקבי ב-max drawdown וביחס תשואה-לסיכון (Sortino/return-to-risk)
לאורך **כמה משטרי שוק**, לא רק תקופה בודדת, בלי פיגור מהותי בתשואה המצטברת או בהתאוששות אחרי המעבר חזרה
ל-Bull — 60 יום בשוק אחד בלבד לא ישכנעו.
**Owner decision:** טרם הוכרע — שני הצדדים ממליצים לא לשנות כרגע ולפתוח מסלול Shadow נפרד כשתהיה החלטה
לבדוק את זה בפועל. ממתין לבעל הריפו אם/מתי לפתוח את המסלול.
**Status:** RESOLVED לעניין התיעוד; ההחלטה האסטרטגית עצמה **OPEN** (ממתינה להחלטה אם לפתוח מסלול Shadow נוסף).

### AI_COUNCIL#6: CRON_SECRET נחשף פעמיים — פעם ב-config.json, פעם בתיעוד/צ'אט
**קטגוריה:** באג מוכח (אבטחה) + כלל תהליכי
**Issue:** תיקון ראשון הוציא סוד מ-`config.json` הציבורי, אבל כתב את הסוד **החדש** במפורש ב-`docs/SECURITY.md`
ובצ'אט — כלומר חשף מחדש בדיוק את מה שהתיקון נועד למנוע.
**Claude position:** טעות אמיתית של Claude, לא רק ביקורת סגנונית. שני הערכים (הישן והחדש) נחשבים חשופים.
**Evidence from code/data:** `docs/SECURITY.md` היסטוריית git — הערך הופיע שם עד commit `e368bee`.
**Counterarguments:** אין — הביקורת הייתה נכונה ב-100%.
**Proposed experiment/fix:** תוקן: **אין ערך סוד פעיל בקבצים הנוכחיים** בריפו. ניסוח מדויק יותר (תוקן בעקבות
AI_COUNCIL#10 למטה): זה לא אומר שהערך "לא קיים בשום מקום" — הוא נשאר לצמיתות בהיסטוריית git ובשיחות עבר
(כולל בצ'אט הזה עצמו), ופשוט נחשב compromised ופסול לשימוש לצמיתות. כלל קבוע (סעיף 8 למעלה) חל גם על
המסמך הזה עצמו.
**Risks:** ההיסטוריה הישנה של git עדיין מכילה את שני הערכים לצמיתות (לא בוצע rewrite — פעולה הרסנית שדורשת
אישור מפורש נפרד). שני הערכים ממילא פסולים לשימוש.
**Status:** RESOLVED (commit `e368bee`) — ממתין לפעולת בעל הריפו (הוספת Secret חדש שלא עובר כאן) לחיבור מחדש.

### AI_COUNCIL#7: נעילת מנוע הסיכון — טענה לא מדויקת על ההגנה בפועל
**קטגוריה:** תיעוד שגוי, לא באג בקוד
**Issue:** תיעוד ראשוני טען ש-`risk-limits-lock.test.mjs` הוא "ההגנה הטכנית האמיתית" מול שינוי סוכן — לא
נכון: כל עוד לסוכן יש push ישיר ל-main, אפשר לשנות את קובץ הסיכון ואת הבדיקה יחד באותו commit.
**Claude position:** התיקון תיקן את עצמו — ההגנה האמיתית היא Branch Protection (עדיין לא מופעלת).
**Evidence from code/data:** `.github/CODEOWNERS`, `invest-api/tests/risk-limits-lock.test.mjs` — שניהם
מתועדים עכשיו עם ההסתייגות הזו במפורש.
**Counterarguments:** אין.
**Proposed experiment/fix:** תוקן תיעוד. הפעולה בפועל (Branch Protection) היא פעולה ידנית של בעל הריפו.
**Risks:** עד שה-Branch Protection יופעל, מגבלות הסיכון בקוד עדיין לא נעולות מפני שינוי מכוון של הסוכן —
רק מפני שינוי בטעות.
**Status:** RESOLVED לעניין התיעוד; **OPEN** לעניין ההפעלה בפועל (ממתין לבעל הריפו).

### AI_COUNCIL#8: קריטריון החלפה למודלים/מסלולים
**קטגוריה:** השערת אסטרטגיה (לא נבדקה עדיין)
**Issue:** regB/regC/אגרסיבי B טרם צברו תקופת מדידה נקייה (התיקונים ב-#1–#4 חייבים היסטוריה שנאספת אחריהם).
**Claude position:** בהתאם ל-`docs/TRACKS.md`/`SHADOW_MODEL.md` — אין להסיק מסקנות לפני ~20 יום, ומשמעותי
רק אחרי ~60. שום מודל לא מחליף אחר בלי החלטה מפורשת מתועדת, גם אז.
**Counterarguments:** אין עדיין — טרם נפתח דיון על תוצאה קונקרטית.
**Proposed experiment/fix:** להמתין למדגם; לעקוב דרך `#/tracks` ו-`GET /tracks/compare`.
**Risks:** תקופת שוק אחת לא מוכיחה כלום לשני הכיוונים.
**Status:** OPEN (ממתין לנתונים)

### AI_COUNCIL#9: CLAUDE.md ("מזג ישירות ל-main") מול הפעלת Branch Protection
**קטגוריה:** החלטת ארכיטקטורה (תהליך עבודה, לא קוד השקעות)
**Issue:** `CLAUDE.md` מורה כרגע "מזג ישירות ל-main, אל תשאל, אל תפתח PR" — בעוד היעד המוצהר (מ-AI_COUNCIL#7,
תגובת בעל הריפו) הוא להפעיל Branch Protection עם Require pull request + Require review from Code Owners.
ברגע שזה יופעל, ההוראה הנוכחית תהיה שגויה טכנית, לא רק לא-רצויה.
**Claude position:** מסכים עם הביקורת, ומוסיף דיוק טכני שחסר בניסוח המקורי: Branch Protection הקלאסי של
GitHub (Require a pull request before merging) הוא **על הענף כולו, לא לפי נתיב קבצים**. אין ל-GitHub מנגנון
סטנדרטי ש"דורש PR רק לקבצי risk/strategy ב-invest-api ומרשה push ישיר לשאר הריפו (english/, remi-wa/,
remi-telegram-free/, app/, chat/, index.html) על אותו ענף main". CODEOWNERS **כן** ניתן להגבלה לפי נתיב —
אבל רק בתוך תהליך PR שכבר חובה; הוא לא יכול להחליף את הדרישה ל-PR עצמה. המשמעות המעשית: הפעלת Branch
Protection על main תעצור push ישיר **לכל האפליקציות בריפו**, לא רק לפלטפורמת ההשקעות.
**Evidence from code/data:** מבנה הריפו (`CLAUDE.md` טבלת "מבנה הריפו") — כל האפליקציות חולקות ענף `main`
יחיד; אין הפרדה בין deploy של `english/`/`remi-wa/` לבין `invest/`/`invest-api/`.
**Counterarguments:** אין מחלוקת מהותית — רק תוספת היקף (scope) שכדאי להיות מודעים אליה לפני ההפעלה.
**עודכן בעקבות AI_COUNCIL#11:** הניסוח המקורי כאן ("האפשרות הנקייה **היחידה**") היה מוחלט מדי. אם בעל
הריפו רוצה PR-required רק לפלטפורמת ההשקעות, יש כמה אפשרויות, כולן יותר מורכבות מהפעלה פשוטה על `main`:
1. **ריפו GitHub נפרד** ל-`invest/`+`invest-api/` עם הגדרות משלו — הכי נקי מבחינת GitHub עצמו, אבל שינוי
   מבני (deploy, GitHub Pages, secrets — הכול עובר).
2. **ענף מוגן נפרד** (למשל `invest-main`) שרק אליו מכוונים ה-workflows של הפריסה (`deploy-invest-api.yml`
   ואולי גם ה-Pages) עם Branch Protection על הענף הזה בלבד, ו-`main` נשאר פתוח לשאר האפליקציות — לא דורש
   ריפו נפרד, אבל דורש לשנות את מנגנון ה-triggers ולוודא ש-GitHub Pages (שמשרת בדרך כלל ענף אחד) עדיין
   מגיש נכון את שאר האתרים.
3. **שינוי מנגנון הפריסה** (deploy ב-Release/workflow_dispatch במקום ב-push ישיר) שמנתק בין "push ל-main"
   ל"קרה deploy", כך שאפשר להגן על `main` בלי לשבש את קצב הפריסה של אפליקציות אחרות.
4. **זהויות/אוטומציה נפרדות** — bot/GitHub App עם bypass מוגבל לצורך CI, בעוד זהות ה-commit הרגילה (כולל
   Claude) לא ברשימת ה-bypass ולכן חייבת PR. **סייג חשוב (בעל הריפו, עודכן בעקבות AI_COUNCIL#11):** אם
   אי-פעם ייבנה bot/GitHub App כזה, אסור לו לקבל bypass על שינויים בקבצי risk/strategy או בקבצים המוגנים
   ב-CODEOWNERS (`engine/risk-limits.js`, `engine/tracks.js`, `engine/aggressive.js`, `engine/autopilot.js`,
   `tests/risk-limits-lock.test.mjs`) — אחרת עוקפים בדיוק את מה שה-Branch Protection נועד למנוע. bypass
   כזה, אם בכלל ייבנה, מוגבל לפעולות deployment/maintenance מוגדרות בלבד (למשל דחיפת קבצי build, לא שינוי
   לוגיקת סיכון/אסטרטגיה).

אף אחת מהן לא "כפתור אחד" כמו הפעלת Branch Protection על `main` כולו — לכן, במבנה הנוכחי של הריפו, ריפו
נפרד עדיין הפתרון **הפשוט** ביותר אם בעתיד ירצו הגנה חזקה רק על מערכת ההשקעות, לא בהכרח ה"יחיד".
**Proposed experiment/fix:** עודכן `CLAUDE.md`: כל עוד Branch Protection לא מופעל — ממשיכה ההוראה הקיימת
(push ישיר, בלי PR). מהרגע שהוא מופעל (פעולה ידנית של בעל הריפו, לא של Claude) — עבור **כל הריפו**: ענף
עבודה → PR → CI ירוק → אישור/סקירה (חובה מ-CODEOWNERS לקבצי risk/strategy) → merge. אין לבצע שינוי אסטרטגי
בפלטפורמת ההשקעות עד שהזרימה הזו פעילה ומאומתת.
**Risks:** אם בעל הריפו רצה בכוונה היקף צר יותר (PR רק ל-invest), התיקון הזה "מרחיב" את ההשפעה על כל
הריפו — סיכון נמוך (זו רק הוראת עבודה ל-Claude, לא שינוי בפועל בהרשאות GitHub), אבל שווה לוודא בהמשך.
**Status:** RESOLVED — בעל הריפו הפעיל Ruleset על `main` (Require pull request, `invest-api tests` כ-required
status check, בלי bypass, force-push/מחיקת ענף חסומים). ה-PR הזה עצמו הוא אימות מקצה-לקצה: נפתח **אחרי**
ההפעלה, כדי לוודא בפועל ש-GitHub חוסם merge לפני שה-check מסיים בהצלחה, ושהוא נפתח אוטומטית ל-PR חדש.

### AI_COUNCIL#10: ניסוח מדויק יותר ל-AI_COUNCIL#6 (מה בדיוק "אין יותר סוד")
**קטגוריה:** תיעוד לא מדויק, לא באג בקוד
**Issue:** הניסוח המקורי ב-#6 ("אין עוד ערך סוד בפועל בשום קובץ/צ'אט") ניתן לקריאה מוטעית כאילו הערך נעלם
מכל מקום. בפועל הוא רק הוסר מהקבצים **הנוכחיים**; הוא נשאר לצמיתות בהיסטוריית git ובשיחות צ'אט עבר.
**Claude position:** מסכים במלואו — התיקון המקורי שלי כבר ציין את זה בשדה Risks של #6, אבל השורה הראשית
(Proposed fix) הייתה לא מדויקת מספיק, וקורא ששוזף רק אותה עלול להסיק מסקנה שגויה.
**Evidence from code/data:** #6 למעלה, שדה Risks המקורי (לפני התיקון) כבר אמר "ההיסטוריה הישנה של git עדיין
מכילה את שני הערכים לצמיתות" — כלומר הסתירה הפנימית הייתה בתוך אותו entry עצמו.
**Counterarguments:** אין.
**Proposed experiment/fix:** תוקן ניסוח #6 למעלה. ניסוח קבוע מעתה (גם נכנס לכלל 8 בכללי העבודה): "אין ערך
סוד פעיל בקבצים הנוכחיים; כל ערך שהופיע אי פעם בקוד, בהיסטוריית git, או בצ'אט נחשב compromised לצמיתות".
**Risks:** אין.
**Status:** RESOLVED (תוקן ישירות בקובץ הזה).

### AI_COUNCIL#11: סימטריה בין Claude ל-ChatGPT בתבנית עצמה
**קטגוריה:** תיעוד/תהליך — מבנה ה-Council עצמו, לא קוד השקעות
**Issue:** התבנית עד עכשיו הציגה "Claude position" ואז "Counterarguments" גנרי (במקום "ChatGPT position"
שווה-מעמד), ובסעיף 10 של כללי העבודה "What would change my mind" זוגי ל-Claude+בעל-הריפו — כלומר בעל
הריפו הוצג כ"מהנדס שני" במקום ChatGPT, וClaude כ"עמדה ראשית". זה לא תואם את הבקשה המקורית: Claude
ו-ChatGPT הם שני מהנדסים שווים, בעל הריפו הוא המכריע, לא צד שלישי בדיון עצמו.
**Claude position:** מסכים לגמרי — זו לא רק בעיית ניסוח, זו סטייה מהותית מהמודל שהתבקש. אין כאן טיעון נגדי.
**ChatGPT position:** לעדכן את התבנית לשדות שווי-מעמד (`Claude position`/`ChatGPT position`,
`What would change Claude's mind`/`What would change ChatGPT's mind`), ולהוסיף `Owner decision` כשדה
נפרד ומובחן משתי העמדות.
**Evidence from code/data:** #5 (לפני התיקון כאן) — "Claude position (אחרי הדיון)" ו-"בעל הריפו" תחת
"What would change my mind", בלי "ChatGPT position" נפרד בכלל; זה בדיוק הדפוס שהביקורת מצביעה עליו.
**Where we agree:** מבנה סימטרי עדיף — הוא הופך את המסמך לכלי שבאמת יכול להפריך את עמדת Claude (או את
עמדת ChatGPT), לא רק לתעד ש-Claude "שקל ביקורת".
**Where we disagree:** אין.
**Proposed experiment/fix:** תבנית עודכנה (למעלה) לעשרה שדות סימטריים; #5 נכתב מחדש לפי התבנית החדשה
כולל עמדת ChatGPT שהועברה על ידי בעל הריפו; #9 עודכן בנפרד (ראה שם) כי הניסוח "האפשרות הנקייה היחידה"
היה מוחלט מדי — ChatGPT צדק שיש עוד אפשרויות (מורכבות יותר) מלבד ריפו נפרד. #1–#4, #6–#8, #10 נשארו
בתבנית הישנה כתיעוד היסטורי (צוין למעלה, ליד התבנית).
**תיקון המשך (אותו נושא, סבב שני):** בסבב הראשון עדכנתי את התבנית ואת #5, אבל **פספסתי** לעדכן את כלל
בית מספר 10 עצמו — הוא המשיך לומר "Claude ובעל הריפו כותבים... What would change my mind", בדיוק הדפוס
שהנושא הזה עצמו קבע כשגוי. ChatGPT/בעל הריפו תפסו את זה נכון: תיקון מבנה שלא מתעדכן בכל מקום שהוא מופיע
הוא תיקון חלקי. כלל 10 תוקן כעת: Claude ו-ChatGPT כותבים "What would change my mind", בעל הריפו ממלא
`Owner decision`. גם נוסף סייג ל-#9 (אופציה 4, bot/bypass): אסור bypass על קבצי risk/strategy/CODEOWNERS —
מוגבל ל-deployment/maintenance מוגדר בלבד.
**Risks:** אין — שינוי מבנה מסמך בלבד.
**What would change Claude's mind:** לא רלוונטי — זו לא השערה שדורשת נתונים, זו תיקון מבנה שמוסכם משני
הצדדים.
**What would change ChatGPT's mind:** לא רלוונטי, מאותה סיבה.
**Owner decision:** אושר על ידי בעל הריפו (זו הבקשה המקורית) — מיושם.
**Status:** RESOLVED (תוקן ישירות בקובץ הזה; #9 עודכן בעקבות זה).

### AI_COUNCIL#12: כיסוי מודל הצל נשאר n=100 אחרי סריקה מלאה של 503/503 — rank לא נוצר כשלא היה קיים
**קטגוריה:** באג מוכח
**Issue:** בבדיקה יומית (2026-09-21) התברר שהריצות המתוזמנות של GitHub Actions לא הופעלו כלל מאז
2026-09-19 (פער של יומיים). הפעלה ידנית (`auto-run` דרך `.github/invest-ops/command`) הצליחה, אבל דוח
מודל הצל הראה `day: 2026-09-20, pipeline: worker, n: 100` — למרות `entry940` שהצליח ל-2026-09-21. הפעלת
`sp500-run` במפורש (PR #9) הריצה סריקה מלאה שדיווחה בעצמה `503/503 עם ציון, EDGAR 503/503, 0 שגיאות,
ingest: written 503`, **אבל** מיד אחר כך `shadow/run?force=1` עדיין החזיר `day: 2026-09-20, n: 100,
pipeline: worker, uniformCount: 0`.
**Claude position:** מסכים לגמרי — זה באג מוכח, לא סינון מכוון, ואימתתי אותו בקריאת קוד ישירה (לא רק
מהלוג), עד שני רבדים: (1) `worker.js`, handler של `POST /ingest/snapshots` עם `finalize=1` — הקוד היה
`if (exRank){ ... }` בלי `else` ליצירת `rank:{day}` כשהוא לא קיים עדיין. זה שונה מ-`cronStep` באותו קובץ
שכבר מטפל נכון במקרה `!exRank` (דרך `putIfAbsent`). (2) `lib/shadow.js`, `runShadow`: `day = day ||
(await latestRankDay(db))` — כש-`rank:2026-09-21` מעולם לא נוצר, `latestRankDay()` המשיך להצביע על
2026-09-20; `runShadow` טען את ה-rank הישן (עם `rank.table` בגודל ~100), ו-`listShardSnaps(db,
'2026-09-20')` (המפתח `snaps:2026-09-20:*`, לא `2026-09-21`) לא מצא את ה-shards שנכתבו תחת היום החדש
→ `uniformCount: 0` → נופל חזרה ל-`pipeline: 'worker'` עם `table = rank.table` הישן (n=100). כלומר ה-100
לא נבע מסינון איכות (DF) אלא מקריאת היום/הדירוג הלא-נכון מלכתחילה — `eligible: 96` מתוך ה-`n: 100` הזה
מחזק את זה (לא "503 → סינון → 100" אלא "100 מההתחלה").
**ChatGPT position:** אותו אבחון בדיוק (התקבל מבעל הריפו): הבאג ב-`finalize` שמטפל רק ב-`if (exRank)`;
הציע 5 תיקונים — (1) ליצור/לעדכן rank גם כש-`exRank` לא קיים, (2)+(3) regression tests לשני המקרים (אין
rank קיים; יש rank חלקי ומוחלף ע"י סריקה מלאה), (4) ב-workflow לקרוא ל-shadow עם `date=$TODAY&force=1`
מפורש במקום להסתמך על latest day, (5) post-condition ב-workflow: אחרי סריקה מלאה shadow.day חייב להיות
היום ו-uniformCount קרוב ליקום המלא — אחרת להתריע, לא לדווח הצלחה שקטה.
**Evidence from code/data:** `worker.js` שורות 310–314 (לפני התיקון); `lib/shadow.js` שורות 8–13
(`runShadow`); `lib/snapstore.js` (`listShardSnaps`/`shardKey` — מפתח `snaps:${day}:${i}`, תלוי-יום);
לוג ריצה 35614120878 (PR #9): `ingest: written 503, skipped 0, rerank false, shards 3` ואז מיד אחריו
`shadow re-run (force)` עם `day: 2026-09-20`.
**Where we agree:** האבחון זהה לחלוטין בין שתי הבדיקות העצמאיות (קוד + לוג בפועל); זה data-pipeline בלבד
— לא נוגע במשקלים, thresholds, או לוגיקת החלטה של אף מודל.
**Where we disagree:** תיקון #4 של ChatGPT (להעביר `date=$TODAY` מפורש מה-workflow ל-`shadow/run`) —
לדעתי מיותר לאחר תיקון #1: ברגע ש-`rank:{day}` נוצר כראוי, `latestRankDay()` יחזיר את היום הנכון ממילא,
ו-`runShadow` בלי פרמטר `day` יפתור אותו נכון. נעילת תאריך מפורש בworkflow יכולה גם להסתיר תרחיש שונה
(שבו דווקא rank של אתמול שלם יותר מהיום החלקי) מאחורי override שמכריח "תמיד היום". לא הוספתי את זה
בכוונה — אם יתגלה עוד מקרה שבו `latestRankDay()` לא מספיק, אפשר לחזור לזה כתיקון נפרד עם נימוק משלו.
**Proposed experiment/fix:** (1) `worker.js`: השורה `if (exRank){ ... }` הוחלפה ב-`if (!exRank ||
overwrite || rank.analyzed > (exRank.analyzed || 0)){ ... }` — מחשב `rank` תמיד ויוצר אותו כשהוא חסר,
באותו דפוס כמו `cronStep`. (2)+(3) שני מבחני רגרסיה חדשים ב-`worker.test.mjs`: אין rank קיים + finalize
→ נוצר; rank חלקי (analyzed=2) + finalize עם סריקה מלאה (8) → מוחלף. (5) `tick-invest.yml`: אחרי
`shadow/run?force=1` בבלוק ה-sp500-nightly, בדיקת שפיות ב-Python — אם `day` שהוחזר שונה מ-`$TODAY` או
`uniformCount < 300`, מדפיס `::warning::` גלוי ב-Actions (לא נכשל את השלב, כדי לא לחסום entry940/autopilot
שאחריו). (4) לא יושם — ראה "Where we disagree".
**Risks:** תיקון #1 קורא ל-`rankSnapshots` תמיד (גם כש-`exRank` קיים ולא משתפר) — עלות חישובית זניחה
(אגרגציה טהורה על snapshots שכבר נטענו, בלי קריאות API נוספות). אין שינוי בסף/משקל/החלטת קנייה-מכירה.
**What would change Claude's mind:** אם יתברר ש-`exRank` ריק במתכוון במקרים מסוימים (לדוגמה placeholder
שנוצר על ידי תהליך אחר לפני שה-snapshots מוכנים) והתיקון גורם ל-rank חלקי/מוקדם מדי להיכתב — זה יצריך
בדיקת `analyzed`/`universeSize` נוספת לפני היצירה, לא רק `!exRank`.
**What would change ChatGPT's mind:** לא ידוע — לא הועבר נימוק נגדי לגבי "Where we disagree" (#4); זה
עדיין פתוח לדיון אם בעל הריפו חושב שנעילת תאריך מפורש כן נחוצה.
**Owner decision:** ―
**Status:** TESTING — תוקן ונבדק (160/160 בדיקות invest-api עוברות, כולל 2 הבדיקות החדשות), ממתין ל-PR
ולריצה אמיתית נוספת של הסריקה הלילית כדי לוודא `day`/`uniformCount` תקינים מקצה-לקצה.
