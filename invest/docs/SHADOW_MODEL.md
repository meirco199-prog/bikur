# מודל הצל — SHADOW_MODEL.md

מודל ציון חדש (Signal Model v8) שרץ **כל לילה במקביל** למודל שמפעיל את האוטומט, **בלי לשלוח אף פקודה**.
העיקרון: מודדים לפני שמחליפים. מנוע הביצוע (v9) והציון הישן מוקפאים לתקופת הניסיון; כל שיפור נבדק כאן על נתונים שנרשמו מראש.
קוד: `invest-api/engine/shadow.js` (טהור), `invest-api/lib/shadow.js` (KV, הרצה לילית), מסך "היום" → כרטיס "מודל צל".

## שלושה מודלים על אותן מניות
| מודל | מה זה | מה נכנס | מה לא |
|---|---|---|---|
| **A** | הישן | הציון והסיגנל של `engine/scoring.js` + `signals.js` (מה שהאוטומט מבצע) | — |
| **B** | יחסי-ענף בלי כפילויות | תמחור 25, רווחיות 20, צמיחה 20, מגמה 25, סיכון 10 — כל גורם = **אחוזון בתוך הענף** (≥5 מניות בענף; אחרת בכל היקום) | מאקרו, סנטימנט, אנליסטים |
| **C** | B + ריוויזיות + מאקרו | כמו B + **ריוויזיות תחזיות EPS ל-30 יום** (15) כשזמינות; **שכבת מאקרו על הפעולה**: שוק דובי → אין קניות, Risk Off → רק קנייה חזקה | — |
| **D** | אגרסיבי | מומנטום 30, צמיחה 25, ריוויזיות 20, איכות 15, סיכון 10 (אחוזונים בתוך הענף); שכבת מאקרו על הפעולה. מפעיל את המסלול האגרסיבי, מושווה ל-C | תמחור, מגמה טכנית, אנליסטים, סנטימנט |

**יקום ייחוס וצינור אחיד**: כל 503 חברות המדד (כולל ה-70 שגם ביקום הבסיס) עוברות בלילה את אותו צינור ב-GitHub Actions (Yahoo + EDGAR, אותו זמן) ונשמרות ב-shards; מודל הצל בונה את הפול שלו מה-shards בלבד (`pipeline:'uniform'`), כך שאחוזוני B/C/D לא משווים נתונים מסוגים/זמנים שונים. ה-Worker ממשיך לנתח את יקום הבסיס בנפרד (נתוני FMP עשירים יותר) לצורך מודל A והאוטומט. האחוזונים מחושבים על חברות ה-S&P 500 בלבד (`universe='sp500'` בטבלת הדירוג) כשיש ≥100 כאלה. היקום המורחב (קרנות, ת"א, מניות מחוץ למדד) מדורג ומוצג, אבל לא משפיע על אחוזוני חברות המדד. גם האוטומט (v10) מחשב את ספי "30%/15% העליונים" מול אותו יקום.

איחוד כפילויות (לפי מטריצת המתאמים שנרשמת כל יום): פונדמנטלי + איכות → **רווחיות** (ROE/ROIC מופיעים בשניהם); טכני + מומנטום → **מגמה**.

## כללים קבועים (`SHADOW_RULES`)
- זכאות: ארבעת הגורמים תמחור/רווחיות/צמיחה/מגמה קיימים. חסר → ציון `null`, לא ממוצע חלקי שנראה כמו ציון.
- פעולה היפותטית לפי דירוג יחסי: קנייה חזקה = 15% העליונים, קנייה = 30%, מכירה = 20% התחתונים, השאר החזקה. פחות מ-20 מניות → הכל החזקה.
- ריוויזיות: `available:false` עד שיש היסטוריה של 30 יום **לאותה שנת כספים** (`fy1Date`); שנה שהתחלפה → משווים ל-FY2 הישן. בלי ריוויזיה המשקל מתחלק בין שאר הגורמים; **לא אפס ולא ערך מומצא**. הרכיב נכנס לציון רק כשיש ≥10 חברות עם ריוויזיה (אחוזון משמעותי).
- ETF/קרנות לא נכללות (הן ליבה, לא מועמדות לוויין).

## מה נרשם (`shadow:{day}`)
לכל מניה: ציוני A/B/C, פעולה לכל מודל, גורמים גולמיים ואחוזונים (יחסית לענף/יקום), **טבלת ייחוס** (`contribC`: תרומת כל גורם לציון C), ריוויזיה או סיבת חוסר, מחיר בסיגנל.
ברמת היום: הסכמה בין המודלים (אותה פעולה; חפיפה ברשימת הקנייה), 10 המובילות לכל מודל, מטריצת מתאמים בין רכיבי המודל הישן + רשימת כפילויות (|r| ≥ 0.6), משטר ושכבת מאקרו.

## תשואות עתידיות ומדידה
- כל לילה, למסמכים של לפני 1 / 5 / 20 / 60 ימי ניתוח, נרשמת תשואת כל מניה **ממחיר 09:40 ניו יורק של יום המסחר שאחרי הסיגנל** (`entryKind:'0940'`, מ-`entry940:{day}` שנאסף ב-GitHub Actions ב-09:45 ET מברים של 5 דקות; `scripts/entry940.mjs`) — כי כך המערכת מבצעת בפועל. אין מחיר 09:40 → פתיחת היום הבא (`next-open`); אין גם פתיחה → סגירת הסיגנל (`close`). תשואה עודפת מול SPY **מאותה נקודת כניסה**. הפתיחה נשמרת גם כסטטיסטיקה משנית (`retOpen`, `actionsOpen`) כדי לראות כמה עשר הדקות הראשונות משנות.
- **D מלא רק עם כיסוי**: ריוויזיות נכנסות לציון רק כשיש ריוויזיה תקינה (אותה שנת כספים, 30 יום) ל-≥75% מהמניות הכשירות ב-D (`revisionCoverageMin`) ולפחות 10. עד אז `D-preRevision`, ורכיב שקיים רק ל-2% מהיקום לא מעוות דירוג יחסי.
- **D-preRevision**: כל עוד ריוויזיות לא זמינות (≥10 חברות עם 30 יום היסטוריה), המשקלים של D מנורמלים בפועל ל-37.5/31.25/18.75/12.5 — זה מודל אחר. הוא נרשם ונצבר תחת המפתח `D-preRevision`; כשהריוויזיות נכנסות נפתח רקורד `D` חדש. לא מערבבים.
- צבירה (`shadow:stats`) לכל אופק ולכל מודל: קבוצות ציון 80+ / 70+ / 60+ / 50+ / מתחת → n, תשואה ממוצעת, עודפת; לפי פעולה (קנייה חזקה/קנייה/החזקה/מכירה); **IC** = מתאם ספירמן יומי בין ציון לתשואה (ממוצע על הימים).
- **מונוטוניות**: האם קבוצת ציון גבוהה יותר הביאה תשואה גבוהה יותר, בסדר, על קבוצות עם ≥5 תצפיות (דורש ≥3 קבוצות). "עוד אין" = לא מספיק נתונים, לא "לא".
- `GET /shadow/report` (היום + סטטיסטיקות), `GET /shadow/{date}` (מסמך מלא), `POST /shadow/run` (טוקן/סוד; `force=1` מחשב מחדש ושומר מילויים קיימים).

## התפלגות ציונים (`shadow:dist`)
כל יום נשמרת לכל מודל התפלגות: ממוצע, חציון, p10/p90, מספר מניות בכל קבוצת ציון, וחלק הפעולות (קנייה חזקה / קנייה / מכירה). סדרה של עד 250 ימים במסמך אחד.
המטרה: לראות אם "קנייה חזקה" נהיית נפוצה מדי או שהציונים זוחלים למעלה עם הזמן (אינפלציית ציונים), בלי שהתשואה העתידית מצדיקה זאת.

## המסלול האגרסיבי (`engine/aggressive.js`, `aggr:state` / `aggr:equity` / `aggr:journal`)
תיק צל שני, סימולציה בלבד (לא PaperBroker, לא חשבון התרגול), שמתחיל ב-200,000 ₪ ורץ כל לילה אחרי מודל הצל על שורות מודל C:
| כלל | ערך |
|---|---|
| ליבה | 20% SPY, לא נמכרת; משלימים כשנופלת מתחת ל-19% |
| מניות | עד 75% מהתיק, עד 10 פוזיציות של 7.5%, ענף ≤ 40% **look-through** (מניות ישירות + SPY × משקל הענף במדד, משקלים משוערים `SPY_SECTOR_WEIGHTS`), עד 5 קניות ביום; מועמדות = פעולה קנייה/קנייה חזקה **במודל D** (הציון האגרסיבי), לפי ציון |
| חשיפה | מדווחת בנפרד: מניות בודדות, קרן SPY, מזומן, וסך חשיפה מנייתית (מניות + קרן). יעד מלא: 75% + 20% = 95% מנייתי |
| יציאה | סיגנל מכירה במודל C; עצירה 12% מהכניסה; עצירה נגררת 15% מהשיא; נייר שיצא מהיקום |
| משטר | שוק דובי: מוכרים את החלשות עד שהמניות ≤ 25% מהתיק, אין קניות; Risk Off: קניות רק לקנייה חזקה |
| אחרי מכירה | לא קונים את אותו נייר בתוך 7 ימים |
| ביצוע | **החלטה בלילה על סגירות → פקודות ממתינות (`pending`) → מילוי למחרת בחלון 09:40–15:45 ניו יורק לפי ציטוט חי** (Twelve Data דרך ה-Worker, ≤ ~10 ציטוטים ביום) + החלקה 10 נ"ב + 4 ₪ לפקודה; נרשם הפער מול מחיר ההחלטה (`gapPct`). בלי ציטוט טרי בחלון → הפקודה מתבטלת בהחלטת הלילה הבא. SPY נרשם באותה נקודת זמן של המילוי הראשון (`spyRef`) כנקודת ההתחלה של המדד. מזומן ≥ 5% |
| מדידה | עקומת הון יומית מול SPY: תשואה, עודף, drawdown מקסימלי, תנודתיות שנתית, ניצחונות/הפסדים |

המסלול לא רודף אחרי יעד. ההשוואה מול חשבון התרגול המאוזן היא על אותם ימים ואותם מחירים. המסלול אופס ב-2026-09-18 עם המעבר למודל D ולמילוי בציטוט חי (הרצות קודמות בארכיון `aggr:archive:*`). ארבעה מספרים נבחנים יחד: תשואה מצטברת, עודף מול SPY, ירידה מקסימלית, תנודתיות. 20 ימים לא מוכיחים כלום לשני הכיוונים; 60 ימים מתחילים לתת אינדיקציה. `GET /aggressive/report`, `POST /aggressive/run`.
ציפייה ריאלית: תיק כזה יכול לרדת 30%–50% בשנה גרועה; אם הוא לא מכה את SPY אחרי עלות הסיכון הזו, הוא לא שווה את זה.

## קריטריון החלפה (החלטה מפורשת, לא אוטומטית)
אחרי לפחות 4–6 שבועות של סיגנלים סגורים (אופק 20 יום): מודל מחליף את A רק אם IC ממוצע גבוה יותר, המונוטוניות מתקיימת, ורשימת הקנייה שלו הכתה את רשימת הקנייה של A בתשואה עודפת. גם אז — ההחלפה היא שינוי גרסה מתועד, לא שינוי שקט.

## מגבלות
- שבועות ספורים = מדגם קטן ותקופת שוק אחת. זה forward test של המכונה, לא הוכחת אלפא. הבדיקה ההיסטורית (10–15 שנים, הרכב מדד היסטורי) נשארת פתוחה.
- **מקור המחירים ל-S&P 500 הוא Yahoo Finance (לא מורשה לאיסוף אוטומטי לפי תנאי השימוש) — מתאים לניסוי הצל בלבד, לא לפרודקשן.** הסקריפט תומך ב-`SP500_PRICE_SOURCE=twelvedata` (מפתח ב-Actions secret `TWELVEDATA_KEY`) ובגיבוי דרך ה-Worker עד 60 סימבולים בלילה, כך ששינוי אצל Yahoo לא ימחק סריקה שלמה. לכסף אמיתי: נתוני שוק מ-IBKR או ספק API מורשה.
- היקום: כל חברי ה-S&P 500 מנותחים ב-GitHub Actions (Yahoo + EDGAR) ונשמרים ב-shards; בשמות האלה אין נתוני FMP (תחזיות/אנליסטים/תאריכי דוחות) — הרכיבים חסרים, לא מומצאים, והריוויזיות זמינות רק לשמות שה-Worker מושך מ-FMP (תקציב 230/יום).
- הריוויזיות מתחילות להיצבר מהיום שבו הוזן מפתח FMP; עד אז מודל C ≈ B + שכבת מאקרו.
